name: Deploy

on:
  workflow_dispatch:

# concurrency:
	# group: ${{ github.ref }}
	# cancel-in-progress: true

jobs:
	test:
		runs-on: ubuntu-latest
		strategy:
			fail-fast: false
	
		steps:
		- name: Test
			run: echo "I am running"
