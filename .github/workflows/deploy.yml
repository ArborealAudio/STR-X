name: Deploy

on:
  workflow_dispatch:

# concurrency:
	# group: ${{ github.ref }}
	# cancel-in-progress: true

jobs:
	build_and_test:
 		name: TEsting
		runs-on: ubuntu-latest
		strategy:
			fail-fast: false
	
		steps:
		- name: Test
			run: echo "I am running"
