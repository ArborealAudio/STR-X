name: Deploy

on:
  workflow_dispatch:

# concurrency:
	# group: ${{ github.ref }}
	# cancel-in-progress: true

jobs:
  build_and_deploy:
 	name: ${{ matrix.os }}
	runs-on: ${{ matrix.os }}
	strategy:
	  fail-fast: false
	  matrix:
		include:
		  - os: ubuntu-20.04
		  cc: clang-12
		  c++: clang++-12

	steps:

    - name: Test
      run: |
        echo "I am running"
